# Dev Spaces Start Workspace

Prototype UI for creating a cloud development workspace. The app focuses on the "Create Workspace" flow and lets users choose a repository, branch, editor, environment components, AI tools, and advanced runtime settings.

## Getting Started

```bash
npm install
```

### Run locally

```bash
npm run dev
```

The local dev server will print the local URL in the terminal.
